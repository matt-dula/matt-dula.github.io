# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Matt Dula
email: dulamatt@msu.edu
description: >- # this means to ignore newlines until "baseurl:"
  PhD candidate focusing on distributed array architectures and signal processing techniques.
  Research notes, publications, and selected essays. 
baseurl: "" # the subpath of your site, e.g. /blog (keep empty for username.github.io)
url: "https://matt-dula.github.io" # the base hostname & protocol for your site, e.g. http://example.com

# Social links (Minima theme supports these natively)
github_username:  matt-dula
# TODO: add linkedin

# >>> Jekyll Settings >>>
theme: minima
markdown: kramdown
highlighter: rouge

include:
  - _pages
exclude:
  - .pixi
  - Gemfile
  - Gemfile.lock

plugins:
  - jekyll-feed
# <<< Jekyll Settings <<<

# >>> Collections >>>
collections:
  news:
    defaults:
      layout: post
    output: true
    permalink: /news/:path/